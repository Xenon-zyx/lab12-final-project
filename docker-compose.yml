services:
  website:
    build: .
    container_name: personal-website
    ports:
      - "80:80"

  todo:
    image: nginx:alpine
    container_name: todo-app
    ports:
      - "3000:80"
    volumes:
      - ./todo:/usr/share/nginx/html:ro